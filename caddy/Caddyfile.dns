{$CADDY_DOMAIN} {
  tls {$CADDY_TLS} {
    dns {$CADDY_DNS_PROVIDER}
  }
  root html/public
  log stdout
  errors stderr

  fastcgi / php-fpm:9000 php
  rewrite {
    to {path} {path}/ /index.php?{query}
  }
}
